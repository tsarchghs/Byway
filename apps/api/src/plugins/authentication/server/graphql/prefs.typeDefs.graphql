#graphql
scalar JSON

extend type User {
  uiPrefs: String
  role: String
}

extend type Query {
  me: User
}

extend type Mutation {
  setUiPref(key: String!, value: String!): Boolean
  setUiPrefs(prefs: String!): Boolean
}