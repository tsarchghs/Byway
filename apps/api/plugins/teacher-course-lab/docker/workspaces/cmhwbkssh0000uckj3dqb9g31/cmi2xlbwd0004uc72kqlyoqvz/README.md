# Lab Frontend Project

This is your lab workspace. Modify `pages/` and `app.vue` to implement the required UI.

## Getting Started

```bash
npm install
npm run dev
```

The app will run on http://localhost:3000
